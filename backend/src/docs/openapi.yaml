openapi: 3.0.0
info:
  title: Amana Backend API
  version: 1.0.0
  description: |-
    OpenAPI coverage for the live Amana backend routes under `backend/src/routes`.

    Authentication flow:
    1. Call `POST /auth/challenge` with a Stellar public key.
    2. Sign the returned challenge with the wallet.
    3. Exchange the signature at `POST /auth/verify` for a bearer token.
    4. Send `Authorization: Bearer <token>` to protected endpoints.

    JWT semantics enforced by middleware:
    - `iss` must match `JWT_ISSUER` (default `amana`)
    - `aud` must match `JWT_AUDIENCE` (default `amana-api`)
    - `jti` is required and checked against the revocation denylist
      (revoked tokens are rejected — replay protection)
    - `iat` (issued-at) must be present and in the past
    - `exp` (expires-at) is enforced: expired tokens are rejected
      (default TTL: 86400 s / 24 h, configurable via `JWT_EXPIRES_IN`)
    - `nbf` (not-before) must be present and not be in the future
    - Tokens signed with algorithms other than HS256 are rejected

    Error code references in operation descriptions map to backend `ErrorCode` values in
    `backend/src/errors/errorCodes.ts`. Some route handlers still emit legacy `{ error }`
    payloads while centralized validation/runtime failures return `{ code, message, details }`.
servers:
  - url: http://localhost:4000
tags:
  - name: Health
  - name: Auth
  - name: Admin
  - name: Wallet
  - name: Users
  - name: Trades
  - name: Disputes
  - name: Dispute Categories
  - name: Manifest
  - name: Evidence
  - name: Audit Trail
  - name: Treasury
  - name: Goals
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    adminAuth:
      type: apiKey
      in: header
      name: x-admin-secret
      description: Secret key authentication for admin-only management routes.
  parameters:
    TradeIdPath:
      in: path
      name: id
      required: true
      description: Trade identifier used by the backend route layer.
      schema:
        type: string
      example: 4294967297
    DisputeIdPath:
      in: path
      name: id
      required: true
      description: Trade identifier for the dispute.
      schema:
        type: string
      example: 4294967297
    DisputeCategoryIdPath:
      in: path
      name: id
      required: true
      description: Dispute category identifier.
      schema:
        type: integer
        minimum: 1
      example: 1
    UserAddressPath:
      in: path
      name: address
      required: true
      description: Stellar public key for the user profile being requested.
      schema:
        type: string
      example: GAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAWHF
    EvidenceCidPath:
      in: path
      name: cid
      required: true
      description: IPFS CID for the stored evidence object.
      schema:
        type: string
      example: bafybeigdyrzt6examplecid
    AuditSignatureQuery:
      in: query
      name: signature
      required: true
      schema:
        type: string
      description: Base64-encoded Ed25519 signature returned with a signed audit export.
    HistoryFormatQuery:
      in: query
      name: format
      required: false
      schema:
        type: string
        enum: [json, csv]
        default: json
      description: Select JSON events or a CSV export.
    HistorySignedQuery:
      in: query
      name: signed
      required: false
      schema:
        type: boolean
        default: false
      description: When true, include canonical payload and integrity metadata.
    PathPaymentSourceAmount:
      in: query
      name: sourceAmount
      required: true
      schema:
        type: string
      example: "1000"
    PathPaymentSourceAsset:
      in: query
      name: sourceAsset
      required: true
      schema:
        type: string
      example: NGN
    PathPaymentSourceAssetIssuer:
      in: query
      name: sourceAssetIssuer
      required: false
      schema:
        type: string
      example: GBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBC4
    ListTradesStatusQuery:
      in: query
      name: status
      required: false
      schema:
        type: string
        enum:
          - CREATED
          - FUNDED
          - DELIVERED
          - RELEASED
          - DISPUTED
          - RESOLVED
    ListTradesPageQuery:
      in: query
      name: page
      required: false
      schema:
        type: integer
        minimum: 1
        default: 1
    ListTradesLimitQuery:
      in: query
      name: limit
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
    ListTradesSortQuery:
      in: query
      name: sort
      required: false
      schema:
        type: string
      example: createdAt:desc
    IncludeInactiveCategoriesQuery:
      in: query
      name: includeInactive
      required: false
      schema:
        type: boolean
        default: false
      description: Include inactive dispute categories in the response.
    IdempotencyKeyHeader:
      in: header
      name: Idempotency-Key
      required: false
      description: Optional idempotency key cached for mutation requests for up to 24 hours.
      schema:
        type: string
    StreamIdPath:
      in: path
      name: id
      required: true
      description: Stream identifier. Must be alphanumeric with optional hyphens or underscores, max 128 characters.
      schema:
        type: string
        minLength: 1
        maxLength: 128
        pattern: "^[a-zA-Z0-9_-]+$"
      example: stream-abc-123
    FeatureNamePath:
      in: path
      name: name
      required: true
      description: Feature flag name. Must be alphanumeric with optional dots, hyphens, or underscores, max 100 characters.
      schema:
        type: string
        minLength: 1
        maxLength: 100
        pattern: "^[a-zA-Z0-9._-]+$"
      example: beta
  schemas:
    ErrorEnvelope:
      type: object
      properties:
        error:
          type: string
      required: [error]
      example:
        error: Unauthorized
    AppErrorResponse:
      type: object
      properties:
        code:
          type: string
          enum:
            - VALIDATION_ERROR
            - AUTH_ERROR
            - DOMAIN_ERROR
            - INFRA_ERROR
            - NOT_FOUND
            - INTERNAL_ERROR
            - CLAWBACK_TOO_LARGE
            - CLAWBACK_INVALID_AMOUNT
            - SUBMISSION_VALIDATION_ERROR
            - SUBMISSION_NETWORK_ERROR
            - SUBMISSION_CONTRACT_ERROR
            - SUBMISSION_AUTHORIZATION_ERROR
          description: |-
            Error code classifying the failure type. Submission error codes:
            - `SUBMISSION_VALIDATION_ERROR`: Malformed transaction XDR (400)
            - `SUBMISSION_NETWORK_ERROR`: Network timeout, connection refused, or rate limiting (429/502/503/504)
            - `SUBMISSION_CONTRACT_ERROR`: Smart contract rejected the transaction (404/502)
            - `SUBMISSION_AUTHORIZATION_ERROR`: Transaction was not authorized (401/403)
        message:
          type: string
        details:
          oneOf:
            - type: object
            - type: array
              items:
                type: object
      required: [code, message, details]
      example:
        code: VALIDATION_ERROR
        message: Validation failed
        details:
          - path: body.amountUsdc
            message: Invalid amount format
    StreamStatus:
      type: string
      description: |-
        Lifecycle state of a vested token stream. ACTIVE and SUSPENDED are live
        states an admin may terminate from; TERMINATED and COMPLETED are terminal.
      enum: [ACTIVE, SUSPENDED, TERMINATED, COMPLETED]
    StreamTerminateRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 500
          description: Operator-supplied reason, stored on the stream and in the admin audit log.
        unsignedTxXdr:
          type: string
          minLength: 1
          description: |-
            Base64 unsigned Soroban transaction XDR for the on-chain terminate
            call. When present it is signed with the admin key and returned as
            `signedTxXdr`; when absent only the backend transition is performed.
      example:
        reason: Recipient offboarded
    StreamTerminateResponse:
      type: object
      properties:
        streamId:
          type: string
        status:
          $ref: "#/components/schemas/StreamStatus"
        previousStatus:
          $ref: "#/components/schemas/StreamStatus"
        terminatedBy:
          type: string
          description: Wallet address of the admin who terminated the stream.
        terminatedAt:
          type: string
          format: date-time
        reason:
          type: string
          nullable: true
        signedTxXdr:
          type: string
          nullable: true
          description: Signed transaction XDR; null when no unsigned XDR was supplied.
        unclaimed:
          type: string
          description: Unclaimed vested amount at the moment of termination.
        reversible:
          type: boolean
          description: Always false — termination cannot be undone.
      required:
        - streamId
        - status
        - previousStatus
        - terminatedBy
        - terminatedAt
        - reversible
      example:
        streamId: stream-abc-123
        status: TERMINATED
        previousStatus: ACTIVE
        terminatedBy: GADMIN000000000000000000000000000000000000000000000000
        terminatedAt: "2026-07-30T12:00:00.000Z"
        reason: Recipient offboarded
        signedTxXdr: null
        unclaimed: "7500"
        reversible: false
    VestingState:
      type: string
      description: |-
        Derived from `claimed` vs. `totalVested` — independent of the stream's
        lifecycle `status`, since e.g. a SUSPENDED stream may already be
        fully vested.
      enum: [not_started, vesting, fully_vested]
    AdminStreamSummary:
      type: object
      properties:
        streamId:
          type: string
        recipient:
          type: string
        status:
          $ref: "#/components/schemas/StreamStatus"
        vestingState:
          $ref: "#/components/schemas/VestingState"
        totalVested:
          type: string
        claimed:
          type: string
        unclaimed:
          type: string
        pendingClawback:
          type: string
        adminTags:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - streamId
        - recipient
        - status
        - vestingState
        - totalVested
        - claimed
        - unclaimed
        - pendingClawback
        - adminTags
        - createdAt
        - updatedAt
      example:
        streamId: stream-abc-123
        recipient: GRECIPIENT000000000000000000000000000000000000000000
        status: ACTIVE
        vestingState: vesting
        totalVested: "10000"
        claimed: "2500"
        unclaimed: "7500"
        pendingClawback: "0"
        adminTags: ["high-value"]
        createdAt: "2026-07-01T00:00:00.000Z"
        updatedAt: "2026-07-05T00:00:00.000Z"
    AdminStreamListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AdminStreamSummary"
        pagination:
          type: object
          properties:
            page:
              type: integer
            limit:
              type: integer
            total:
              type: integer
            totalPages:
              type: integer
          required: [page, limit, total, totalPages]
      required: [items, pagination]
    StreamClawbackPreviewRequest:
      type: object
      properties:
        amount:
          type: string
          pattern: "^\\d+$"
          description: Requested clawback amount as a positive integer string.
      required: [amount]
      example:
        amount: "3000"
    StreamClawbackPreviewResponse:
      type: object
      properties:
        streamId:
          type: string
        remainingVested:
          type: string
          description: The stream's actual unclaimed vested balance before this clawback.
        requestedClawback:
          type: string
        postClawbackBalance:
          type: string
          description: remainingVested minus requestedClawback.
        preview:
          type: boolean
        timestamp:
          type: string
          format: date-time
      required:
        - streamId
        - remainingVested
        - requestedClawback
        - postClawbackBalance
        - preview
        - timestamp
      example:
        streamId: stream-abc-123
        remainingVested: "7500"
        requestedClawback: "3000"
        postClawbackBalance: "4500"
        preview: true
        timestamp: "2026-07-30T12:00:00.000Z"
    StreamLockRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 500
          description: Operator-supplied reason for the maintenance lock.
      example:
        reason: Scheduled database migration
    StreamLockResponse:
      type: object
      properties:
        streamId:
          type: string
        locked:
          type: boolean
          description: Always true for a successful lock.
        lockedBy:
          type: string
          description: Wallet address of the admin who locked the stream.
        lockedAt:
          type: string
          format: date-time
        reason:
          type: string
          nullable: true
      required:
        - streamId
        - locked
        - lockedBy
        - lockedAt
      example:
        streamId: stream-abc-123
        locked: true
        lockedBy: GADMIN000000000000000000000000000000000000000000000000
        lockedAt: "2026-07-30T12:00:00.000Z"
        reason: Scheduled database migration
    StreamUnlockRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 500
          description: Optional note for the unlock action.
      example:
        reason: Maintenance complete
    StreamUnlockResponse:
      type: object
      properties:
        streamId:
          type: string
        locked:
          type: boolean
          description: Always false for a successful unlock.
        unlockedBy:
          type: string
          description: Wallet address of the admin who unlocked the stream.
        unlockedAt:
          type: string
          format: date-time
        reason:
          type: string
          nullable: true
      required:
        - streamId
        - locked
        - unlockedBy
        - unlockedAt
      example:
        streamId: stream-abc-123
        locked: false
        unlockedBy: GADMIN000000000000000000000000000000000000000000000000
        unlockedAt: "2026-07-30T12:00:00.000Z"
        reason: null
    AuthChallengeRequest:
      type: object
      properties:
        walletAddress:
          type: string
          description: Stellar Ed25519 public key.
      required: [walletAddress]
      example:
        walletAddress: GAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAWHF
    AuthChallengeResponse:
      type: object
      properties:
        challenge:
          type: string
      required: [challenge]
      example:
        challenge: amana:login:1742794421:7ced1c65a9a44a7d
    AuthVerifyRequest:
      type: object
      properties:
        walletAddress:
          type: string
        signedChallenge:
          type: string
          description: Base64url wallet signature for the issued challenge.
      required: [walletAddress, signedChallenge]
      example:
        walletAddress: GAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAWHF
        signedChallenge: vj3mDq2F8wYl9d3r2rYzQhC7Z7Cexample
    AuthVerifyResponse:
      type: object
      properties:
        token:
          type: string
          description: JWT bearer token for subsequent API calls.
      required: [token]
      example:
        token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example
    LogoutResponse:
      type: object
      properties:
        message:
          type: string
      required: [message]
      example:
        message: Logged out successfully
    HealthIndicator:
      type: object
      properties:
        status:
          type: string
          enum: [up, down]
        message:
          type: string
        responseTime:
          type: number
      required: [status, message, responseTime]
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum: [healthy, degraded, unhealthy]
        timestamp:
          type: string
          format: date-time
        uptime:
          type: number
        checks:
          type: object
          properties:
            database:
              $ref: "#/components/schemas/HealthIndicator"
            redis:
              $ref: "#/components/schemas/HealthIndicator"
            indexer:
              $ref: "#/components/schemas/HealthIndicator"
            stellar:
              $ref: "#/components/schemas/HealthIndicator"
            ipfs:
              $ref: "#/components/schemas/HealthIndicator"
            redis:
              $ref: "#/components/schemas/HealthIndicator"
            config:
              $ref: "#/components/schemas/HealthIndicator"
          required: [database, indexer, stellar, ipfs, redis, config]
        details:
          type: object
          properties:
            databaseLatency:
              type: number
            redisLatency:
              type: number
            indexerLagSeconds:
              type: number
            lastProcessedLedger:
              type: integer
              nullable: true
            stellarNetwork:
              type: string
            ipfsGateway:
              type: string
            missingEnvVars:
              type: array
              items:
                type: string
          required:
            [databaseLatency, redisLatency, indexerLagSeconds, lastProcessedLedger, stellarNetwork, ipfsGateway, missingEnvVars]
      required: [status, timestamp, uptime, checks, details]
    LivenessResponse:
      type: object
      properties:
        status:
          type: string
          enum: [alive]
        timestamp:
          type: string
          format: date-time
      required: [status, timestamp]
    ReadinessResponse:
      type: object
      properties:
        status:
          type: string
          enum: [ready, not_ready]
        timestamp:
          type: string
          format: date-time
        checks:
          type: object
          properties:
            database:
              $ref: "#/components/schemas/HealthIndicator"
            redis:
              $ref: "#/components/schemas/HealthIndicator"
            indexer:
              $ref: "#/components/schemas/HealthIndicator"
          required: [database, redis, indexer]
      required: [status, timestamp]
    WalletBalanceResponse:
      type: object
      properties:
        balance:
          type: string
        asset:
          type: string
      required: [balance, asset]
      example:
        balance: "150.50"
        asset: USDC
    PathPaymentRoute:
      type: object
      additionalProperties: true
      example:
        source_amount: "1000"
        source_asset_type: native
        source_asset_code: NGN
        destination_amount: "1"
        destination_asset_type: credit_alphanum4
        destination_asset_code: USDC
        path: []
    PathPaymentQuoteResponse:
      type: object
      properties:
        routes:
          type: array
          items:
            $ref: "#/components/schemas/PathPaymentRoute"
      required: [routes]
    UserProfile:
      type: object
      additionalProperties: true
      properties:
        address:
          type: string
        display_name:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
          nullable: true
      required: [address]
    UpdateProfileRequest:
      type: object
      properties:
        displayName:
          type: string
          minLength: 2
          maxLength: 32
        avatarUrl:
          type: string
          format: uri
      additionalProperties: false
      example:
        displayName: Amana Trader
        avatarUrl: https://cdn.example.com/avatar.png
    TradeMutationRequest:
      type: object
      properties:
        sellerAddress:
          type: string
        amountUsdc:
          oneOf:
            - type: string
            - type: number
        buyerLossBps:
          type: integer
          minimum: 0
          maximum: 10000
        sellerLossBps:
          type: integer
          minimum: 0
          maximum: 10000
      required: [sellerAddress, amountUsdc, buyerLossBps, sellerLossBps]
      example:
        sellerAddress: GBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBC4
        amountUsdc: "125.1234567"
        buyerLossBps: 5000
        sellerLossBps: 5000
    TradeMutationResponse:
      type: object
      properties:
        tradeId:
          type: string
        unsignedXdr:
          type: string
      required: [tradeId, unsignedXdr]
    UnsignedXdrResponse:
      type: object
      properties:
        unsignedXdr:
          type: string
      required: [unsignedXdr]
    TradeSummary:
      type: object
      additionalProperties: true
      properties:
        tradeId:
          type: string
        buyerAddress:
          type: string
        sellerAddress:
          type: string
        amountUsdc:
          type: string
        status:
          type: string
      required: [tradeId]
    TradeListResponse:
      type: object
      additionalProperties: true
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/TradeSummary"
      required: [items]
    TradeStatsResponse:
      type: object
      additionalProperties: true
    DisputeCategory:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
          maxLength: 100
        description:
          type: string
          nullable: true
        isActive:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, name, isActive, createdAt, updatedAt]
    DisputeCategoryListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/DisputeCategory"
      required: [items]
    DisputeResponse:
      type: object
      properties:
        id:
          type: integer
        tradeId:
          type: string
        initiator:
          type: string
        reason:
          type: string
        status:
          type: string
          enum: [OPEN, UNDER_REVIEW, RESOLVED, CLOSED]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        resolvedAt:
          type: string
          format: date-time
          nullable: true
        trade:
          type: object
          properties:
            buyerAddress:
              type: string
            sellerAddress:
              type: string
            amountUsdc:
              type: string
      required:
        [id, tradeId, initiator, reason, status, createdAt, updatedAt, trade]
    DisputeListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/DisputeResponse"
        pagination:
          type: object
          properties:
            page:
              type: integer
            limit:
              type: integer
            total:
              type: integer
            totalPages:
              type: integer
          required: [page, limit, total, totalPages]
      required: [items, pagination]
    DisputeTransitionRequest:
      type: object
      properties:
        status:
          type: string
          enum: [UNDER_REVIEW, RESOLVED, CLOSED]
      required: [status]
      example:
        status: UNDER_REVIEW
    DisputeCategoryMutationRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: string
          maxLength: 1000
        isActive:
          type: boolean
      required: [name]
      example:
        name: DAMAGE
        description: Goods arrived damaged or unusable.
    DisputeCategoryUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: string
          maxLength: 1000
        isActive:
          type: boolean
      example:
        isActive: false
    ManifestRequest:
      type: object
      properties:
        driverName:
          type: string
        driverIdNumber:
          type: string
        vehicleRegistration:
          type: string
        routeDescription:
          type: string
        expectedDeliveryAt:
          type: string
          format: date-time
      required:
        - driverName
        - driverIdNumber
        - vehicleRegistration
        - routeDescription
        - expectedDeliveryAt
    ManifestSubmissionResponse:
      type: object
      properties:
        manifestId:
          oneOf:
            - type: string
            - type: integer
        unsignedXdr:
          type: string
      required: [manifestId, unsignedXdr]
    ManifestView:
      type: object
      additionalProperties: true
      description: |
        Returned shape depends on caller role.
        - Seller views include full manifest fields.
        - Buyer views mask driver identity fields.
        - Mediator/admin views expose hashed identity values.
      properties:
        tradeId:
          type: string
        roleView:
          type: string
      required: [tradeId]
    EvidenceListResponse:
      type: object
      properties:
        evidence:
          type: array
          items:
            type: object
            additionalProperties: true
      required: [evidence]
    EvidenceUploadResponse:
      type: object
      additionalProperties: true
      example:
        cid: bafybeigdyrzt6examplecid
        tradeId: 3f6e7d1b-1a2b-4c3d-9e8f-123456789abc
    AuditEvent:
      type: object
      properties:
        eventType:
          type: string
        timestamp:
          type: string
          format: date-time
        actor:
          type: string
        metadata:
          type: object
          additionalProperties: true
      required: [eventType, timestamp, actor, metadata]
    SignedAuditIntegrity:
      type: object
      additionalProperties: true
      properties:
        signature:
          type: string
        algorithm:
          type: string
        keyId:
          type: string
          nullable: true
    AuditHistoryResponse:
      type: object
      properties:
        format:
          type: string
          enum: [json, csv]
        events:
          type: array
          items:
            $ref: "#/components/schemas/AuditEvent"
        csv:
          type: string
        integrity:
          $ref: "#/components/schemas/SignedAuditIntegrity"
        canonicalPayload:
          type: object
          additionalProperties: true
          nullable: true
      required: [format]
    AuditVerifyResponse:
      type: object
      properties:
        valid:
          type: boolean
        payloadHash:
          type: string
        algorithm:
          type: string
        keyId:
          type: string
          nullable: true
      required: [valid, payloadHash, algorithm, keyId]
    GoalsAnalyticsResponse:
      type: object
      properties:
        walletAddress:
          type: string
        totalGoals:
          type: integer
        activeGoals:
          type: integer
        completedGoals:
          type: integer
        totalTargetAmount:
          type: string
        totalCurrentAmount:
          type: string
        overallProgressPercentage:
          type: number
        goals:
          type: array
          items:
            type: object
            properties:
              goalId:
                type: string
              targetAmountUsdc:
                type: string
              currentAmountUsdc:
                type: string
              percentageComplete:
                type: number
              daysRemaining:
                type: integer
              deadline:
                type: string
                format: date-time
              status:
                type: string
              vaultBalance:
                type: string
              isOnTrack:
                type: boolean
            required:
              - goalId
              - targetAmountUsdc
              - currentAmountUsdc
              - percentageComplete
              - daysRemaining
              - deadline
              - status
              - vaultBalance
              - isOnTrack
        timestamp:
          type: string
          format: date-time
      required:
        - walletAddress
        - totalGoals
        - activeGoals
        - completedGoals
        - totalTargetAmount
        - totalCurrentAmount
        - overallProgressPercentage
        - goals
        - timestamp
  responses:
    UnauthorizedError:
      description: Missing, invalid, revoked, or semantically invalid bearer token.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            unauthorized:
              value:
                error: Unauthorized
            revoked:
              value:
                error: "Unauthorized: token has been revoked"
    ForbiddenError:
      description: Authenticated caller is not allowed to perform the action.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          example:
            error: Forbidden
    NotFoundError:
      description: Requested resource was not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          example:
            error: Trade not found
    LegacyValidationError:
      description: Request payload failed route-level validation.
      content:
        application/json:
          schema:
            oneOf:
              - $ref: "#/components/schemas/ErrorEnvelope"
              - $ref: "#/components/schemas/AppErrorResponse"
    InternalError:
      description: Unexpected backend failure.
      content:
        application/json:
          schema:
            oneOf:
              - $ref: "#/components/schemas/ErrorEnvelope"
              - $ref: "#/components/schemas/AppErrorResponse"
paths:
  /api/admin/audit:
    get:
      tags: [Admin]
      summary: Query administrative audit log events
      description: Retrieve paginated administrative audit logs with optional filter parameters.
      security:
        - adminAuth: []
      responses:
        "200":
          description: Successfully retrieved admin audit log entries.
          content:
            application/json:
              schema:
                type: object
                properties:
                  logs:
                    type: array
                    items:
                      type: object
        "401":
          $ref: "#/components/responses/UnauthorizedError"
  /api/admin/features:
    get:
      tags: [Admin]
      summary: List system feature flags
      description: Get active feature flags and system runtime toggles for administrative controls.
      security:
        - adminAuth: []
      responses:
        "200":
          description: Feature flag status map returned.
        "401":
          $ref: "#/components/responses/UnauthorizedError"
  /api/admin/streams:
    get:
      tags: [Admin]
      summary: List admin-accessible streams (#51)
      description: |-
        Paginated list of vested token streams, newest first. Supports
        filtering by lifecycle `status`, derived `vestingState` (how much of
        the stream has actually vested, independent of lifecycle status),
        and `adminTag` (operator-assigned label). Backs the admin stream
        list UI that the clawback confirmation flow (#56, #57) is launched
        from.
      security:
        - bearerAuth: []
        - adminAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/StreamStatus"
        - name: vestingState
          in: query
          schema:
            $ref: "#/components/schemas/VestingState"
        - name: adminTag
          in: query
          schema:
            type: string
            minLength: 1
            maxLength: 100
      responses:
        "200":
          description: Paginated stream summaries.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminStreamListResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/admin/streams/{id}:
    get:
      tags: [Admin]
      summary: Get stream state by ID
      description: |-
        Retrieve cached stream state for admin queries. Returns cached result
        when fresh, otherwise fetches from DB and caches it.

        Error code references: `NOT_FOUND` (unknown stream), `VALIDATION_ERROR` (invalid stream ID format).
      security:
        - bearerAuth: []
        - adminAuth: []
      parameters:
        - $ref: "#/components/parameters/StreamIdPath"
      responses:
        "200":
          description: Stream state.
          content:
            application/json:
              schema:
                type: object
        "400":
          description: Invalid stream ID format.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/admin/streams/{id}/clawback/preview:
    post:
      tags: [Admin]
      summary: Preview a stream clawback before execution
      description: |-
        Validates a requested clawback amount against the stream's actual
        remaining vested (unclaimed) balance and returns the resulting
        post-clawback balance, without performing the clawback. Powers the
        admin confirmation modal (#56) and client-side amount validation
        (#57) — the frontend calls this before letting the operator confirm.

        Concurrent clawback requests against the same stream are serialized;
        a request against a stream with a clawback already in flight gets
        `409`. All routes in this section return `404` when the
        `ADMIN_ROUTES_ENABLED` feature flag is off.

        Error code references: `NOT_FOUND` (unknown stream),
        `CLAWBACK_INVALID_AMOUNT` (zero/negative amount),
        `CLAWBACK_TOO_LARGE` (amount exceeds `remainingVested`),
        `DOMAIN_ERROR` (clawback already in progress for this stream).
      security:
        - bearerAuth: []
        - adminAuth: []
      parameters:
        - $ref: "#/components/parameters/StreamIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamClawbackPreviewRequest"
      responses:
        "200":
          description: Clawback preview computed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreamClawbackPreviewResponse"
        "400":
          description: Invalid stream ID format, invalid amount, or amount exceeds the remaining vested balance.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          description: A clawback is already in progress for this stream.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/admin/streams/{id}/terminate:
    post:
      tags: [Admin]
      summary: Terminate a stream (admin only)
      description: |-
        Permanently terminates a vested token stream. Admin only.

        The stream's state is validated before anything is signed or written:
        an unknown stream is a `404`, and a stream already in a terminal state
        (`TERMINATED`, `COMPLETED`) is a `409` rather than a silent no-op, so a
        double-submit is visible to the operator instead of producing a second
        audit record for one real action.

        Supplying `unsignedTxXdr` signs the on-chain terminate call with the
        configured admin key and returns the signed XDR for submission; the
        resulting on-chain event is ingested by the event listener. Omitting it
        performs the backend state transition only. Every successful
        termination writes an `AdminActionAudit` record (`STREAM_TERMINATE`).

        Termination is irreversible — `reversible` is always `false`.
        Error code references: `NOT_FOUND`, `DOMAIN_ERROR`, `VALIDATION_ERROR`.
      security:
        - bearerAuth: []
        - adminAuth: []
      parameters:
        - $ref: "#/components/parameters/StreamIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamTerminateRequest"
      responses:
        "200":
          description: Stream terminated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreamTerminateResponse"
        "400":
          description: Invalid stream ID format or validation error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          description: Stream is not in a state that can be terminated, or the stream is locked for maintenance.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/admin/streams/{id}/lock:
    post:
      tags: [Admin]
      summary: Lock a stream for maintenance (admin only)
      description: |-
        Marks a stream as locked for operational maintenance. Idempotent —
        locking an already-locked stream returns 200 without changing state.

        Locked streams reject other admin mutations (suspend, resume,
        terminate, clawback) with `409 Conflict` until unlocked.
        User-facing operations are unaffected.

        Every successful lock writes an `AdminActionAudit` record
        (`STREAM_LOCK`). Error code references: `NOT_FOUND`, `VALIDATION_ERROR`.
      security:
        - bearerAuth: []
        - adminAuth: []
      parameters:
        - $ref: "#/components/parameters/StreamIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamLockRequest"
      responses:
        "200":
          description: Stream locked (or already locked).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreamLockResponse"
        "400":
          description: Invalid stream ID format or validation error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/admin/streams/{id}/unlock:
    post:
      tags: [Admin]
      summary: Unlock a previously locked stream (admin only)
      description: |-
        Removes the maintenance lock from a stream. Idempotent — unlocking an
        already-unlocked stream returns 200 without changing state.

        Every successful unlock writes an `AdminActionAudit` record
        (`STREAM_UNLOCK`). Error code references: `NOT_FOUND`, `VALIDATION_ERROR`.
      security:
        - bearerAuth: []
        - adminAuth: []
      parameters:
        - $ref: "#/components/parameters/StreamIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StreamUnlockRequest"
      responses:
        "200":
          description: Stream unlocked (or already unlocked).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreamUnlockResponse"
        "400":
          description: Invalid stream ID format or validation error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /health:
    get:
      tags: [Health]
      summary: Run a comprehensive health check
      description: |-
        Returns composite health state for database, Redis cache, and indexer dependencies.
        Error code references: `INFRA_ERROR`, `INTERNAL_ERROR`.
      responses:
        "200":
          description: Service is healthy or degraded but still responding.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        "503":
          description: Service is unhealthy or a health dependency check failed.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/HealthResponse"
                  - $ref: "#/components/schemas/ErrorEnvelope"
  /health/live:
    get:
      tags: [Health]
      summary: Check process liveness
      responses:
        "200":
          description: API process is running.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LivenessResponse"
  /health/ready:
    get:
      tags: [Health]
      summary: Check readiness to serve traffic
      responses:
        "200":
          description: Service is ready to accept requests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessResponse"
        "503":
          description: Service is not ready.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ReadinessResponse"
                  - $ref: "#/components/schemas/ErrorEnvelope"
  /health/startup:
    get:
      tags: [Health]
      summary: Check startup readiness
      description: |-
        Lightweight probe for slow-starting containers. Checks only core
        dependencies (database, redis) without requiring the indexer to
        have processed events yet.
      responses:
        "200":
          description: Service has started successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessResponse"
        "503":
          description: Service has not finished starting.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ReadinessResponse"
                  - $ref: "#/components/schemas/ErrorEnvelope"
  /auth/challenge:
    post:
      tags: [Auth]
      summary: Issue a wallet-signing challenge
      description: |-
        Rate limited to 10 attempts per 15-minute window per IP.
        Error code references: `VALIDATION_ERROR`, `AUTH_ERROR`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthChallengeRequest"
      responses:
        "200":
          description: Challenge generated successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthChallengeResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "429":
          description: Rate limit exceeded for challenge creation.
          content:
            application/json:
              schema:
                oneOf:
                  - type: string
                  - $ref: "#/components/schemas/ErrorEnvelope"
              example: Too many challenges/verify attempts, try again later.
  /auth/verify:
    post:
      tags: [Auth]
      summary: Verify the signed challenge and issue a JWT
      description: |-
        Rate limited to 10 attempts per 15-minute window per IP.
        Error code references: `VALIDATION_ERROR`, `AUTH_ERROR`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthVerifyRequest"
      responses:
        "200":
          description: Signature verified and JWT issued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthVerifyResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "429":
          description: Rate limit exceeded for verification.
          content:
            application/json:
              schema:
                oneOf:
                  - type: string
                  - $ref: "#/components/schemas/ErrorEnvelope"
              example: Too many challenges/verify attempts, try again later.
  /auth/logout:
    post:
      tags: [Auth]
      summary: Revoke the caller's JWT
      description: |-
        Adds the current token `jti` to the revocation denylist until expiry.
        Error code references: `AUTH_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Logout completed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogoutResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
  /wallet/balance:
    get:
      tags: [Wallet]
      summary: Fetch the authenticated wallet's token balance
      description: "Error code references: `AUTH_ERROR`, `INFRA_ERROR`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Balance retrieved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WalletBalanceResponse"
        "400":
          description: JWT was present but no wallet address claim was available.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                error: Wallet address not found in token
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
  /wallet/path-payment-quote:
    get:
      tags: [Wallet]
      summary: Fetch Stellar path payment quotes
      description: "Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `INFRA_ERROR`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/PathPaymentSourceAmount"
        - $ref: "#/components/parameters/PathPaymentSourceAsset"
        - $ref: "#/components/parameters/PathPaymentSourceAssetIssuer"
      responses:
        "200":
          description: Quote routes returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PathPaymentQuoteResponse"
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                error: Missing sourceAmount or sourceAsset
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
  /users/me:
    get:
      tags: [Users]
      summary: Fetch the authenticated user's profile
      description: "Error code references: `AUTH_ERROR`, `INFRA_ERROR`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Profile loaded or created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
    put:
      tags: [Users]
      summary: Update the authenticated user's profile
      description: "Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `INFRA_ERROR`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProfileRequest"
      responses:
        "200":
          description: Profile updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
  /users/{address}:
    get:
      tags: [Users]
      summary: Fetch a public profile by Stellar address
      description: "Error code references: `NOT_FOUND`, `INFRA_ERROR`, `INTERNAL_ERROR`."
      parameters:
        - $ref: "#/components/parameters/UserAddressPath"
      responses:
        "200":
          description: Public profile returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "404":
          description: No public profile exists for the provided address.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                error: User not found
        "500":
          $ref: "#/components/responses/InternalError"
  /dispute-categories:
    get:
      tags: [Dispute Categories]
      summary: List dispute categories
      description: Returns active categories by default. Admin wallets can include inactive categories.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IncludeInactiveCategoriesQuery"
      responses:
        "200":
          description: Dispute categories returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisputeCategoryListResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Dispute Categories]
      summary: Create a dispute category
      description: Admin wallet access is required.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisputeCategoryMutationRequest"
      responses:
        "201":
          description: Dispute category created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisputeCategory"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "409":
          description: Category name already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
  /dispute-categories/{id}:
    get:
      tags: [Dispute Categories]
      summary: Fetch a dispute category
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/DisputeCategoryIdPath"
      responses:
        "200":
          description: Dispute category returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisputeCategory"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      tags: [Dispute Categories]
      summary: Update a dispute category
      description: Admin wallet access is required.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/DisputeCategoryIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisputeCategoryUpdateRequest"
      responses:
        "200":
          description: Dispute category updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisputeCategory"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          description: Category name already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Dispute Categories]
      summary: Deactivate a dispute category
      description: Admin wallet access is required. The category is marked inactive.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/DisputeCategoryIdPath"
      responses:
        "204":
          description: Dispute category deactivated.
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /disputes:
    get:
      tags: [Disputes]
      summary: List disputes for mediators
      description: |-
        Returns paginated list of disputes accessible to the authenticated mediator.
        Error code references: `AUTH_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: status
          required: false
          schema:
            type: string
            enum: [OPEN, UNDER_REVIEW, RESOLVED, CLOSED]
          description: Filter by dispute status
        - in: query
          name: page
          required: false
          schema:
            type: integer
            minimum: 1
            default: 1
          description: Page number for pagination
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 10
          description: Number of items per page
      responses:
        "200":
          description: Dispute list returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisputeListResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          description: Caller is not a mediator.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
  /disputes/{id}/transition:
    post:
      tags: [Disputes]
      summary: Transition dispute status
      description: |-
        Mediator-only endpoint to transition a dispute to a new status.
        Valid transitions: OPEN → UNDER_REVIEW → RESOLVED/CLOSED
        Error code references: `AUTH_ERROR`, `DISPUTE_NOT_FOUND`, `DISPUTE_STATUS_TRANSITION_INVALID`, `DISPUTE_STATUS_CONFLICT`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/DisputeIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisputeTransitionRequest"
      responses:
        "200":
          description: Dispute status transitioned successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisputeResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          description: Caller is not a mediator.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          description: Dispute not found for the trade.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "409":
          description: Dispute was modified by another user (optimistic concurrency conflict).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /trades:
    post:
      tags: [Trades]
      summary: Create a new trade and build the create-trade XDR
      description: |-
        The buyer address is derived from the bearer token, not the request body.
        Provide optional `Idempotency-Key` for safe retries.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradeMutationRequest"
      responses:
        "201":
          description: Trade created and unsigned contract XDR returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradeMutationResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
    get:
      tags: [Trades]
      summary: List the authenticated user's trades
      description: "Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ListTradesStatusQuery"
        - $ref: "#/components/parameters/ListTradesPageQuery"
        - $ref: "#/components/parameters/ListTradesLimitQuery"
        - $ref: "#/components/parameters/ListTradesSortQuery"
      responses:
        "200":
          description: Paginated trade list returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradeListResponse"
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
  /trades/stats:
    get:
      tags: [Trades]
      summary: Fetch aggregate trade stats for the authenticated user
      description: "Error code references: `AUTH_ERROR`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Trade stats returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradeStatsResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
  /trades/{id}:
    get:
      tags: [Trades]
      summary: Fetch a trade by identifier
      description: "Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
      responses:
        "200":
          description: Trade returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradeSummary"
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
  /trades/{id}/deposit:
    post:
      tags: [Trades]
      summary: Build a deposit transaction for a trade
      description: |-
        Only the buyer can build the deposit transaction, and the trade must still be in `CREATED`.
        Supports optional `Idempotency-Key`.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      responses:
        "200":
          description: Unsigned deposit transaction returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnsignedXdrResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /trades/{id}/confirm:
    post:
      tags: [Trades]
      summary: Build a confirm-delivery transaction
      description: |-
        Only the buyer can confirm delivery, and the trade must be `FUNDED`.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
      responses:
        "200":
          description: Unsigned confirm-delivery transaction returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnsignedXdrResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          description: Caller is not the buyer or is otherwise denied access.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              examples:
                buyerOnly:
                  value:
                    error: Only the buyer may confirm delivery
                generic:
                  value:
                    error: Forbidden
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /trades/{id}/release:
    post:
      tags: [Trades]
      summary: Build a release-funds transaction
      description: |-
        Allowed for the buyer or an admin wallet only, and the trade must be `DELIVERED`.
        Supports optional `Idempotency-Key`.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      responses:
        "200":
          description: Unsigned release transaction returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnsignedXdrResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          description: Caller is not allowed to release funds.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              examples:
                buyerOrAdmin:
                  value:
                    error: Only the buyer or an admin may release funds
                generic:
                  value:
                    error: Forbidden
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /trades/{id}/dispute:
    post:
      tags: [Trades]
      summary: Initiate a dispute for a trade
      description: |-
        Supports optional `Idempotency-Key`.
        Route validation requires a reason of at least 10 characters and either `category`
        or `categoryId`. The category must match an active dispute category.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  minLength: 10
                category:
                  type: string
                  description: Active dispute category name.
                categoryId:
                  type: integer
                  minimum: 1
                  description: Active dispute category id.
              required: [reason]
            example:
              reason: Goods arrived damaged at delivery point.
              category: DAMAGE
      responses:
        "200":
          description: Unsigned dispute transaction returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnsignedXdrResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /trades/{id}/manifest:
    get:
      tags: [Manifest]
      summary: Fetch the manifest view for a trade
      description: |-
        Returned fields depend on caller role:
        - seller receives the full manifest
        - buyer receives masked driver identity
        - mediator/admin receives hashed identity values
        Error code references: `AUTH_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
      responses:
        "200":
          description: Manifest returned for the caller's role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManifestView"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Manifest]
      summary: Submit a manifest and build the submit-manifest XDR
      description: "Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ManifestRequest"
      responses:
        "201":
          description: Manifest accepted and unsigned contract XDR returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManifestSubmissionResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          description: A manifest already exists for the trade.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
  /trades/{id}/evidence:
    get:
      tags: [Evidence]
      summary: List evidence records for a trade
      description: "Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
      responses:
        "200":
          description: Evidence records returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceListResponse"
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          $ref: "#/components/responses/NotFoundError"
  /evidence/{cid}/stream:
    get:
      tags: [Evidence]
      summary: Stream evidence from IPFS through the backend proxy
      description: |-
        Supports byte-range requests using the inbound `Range` header.
        Successful range requests return `206 Partial Content` and set `accept-ranges: bytes`.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `INFRA_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceCidPath"
      responses:
        "200":
          description: Full evidence stream returned.
          content:
            video/mp4:
              schema:
                type: string
                format: binary
            video/webm:
              schema:
                type: string
                format: binary
        "206":
          description: Partial content stream returned for a range request.
          content:
            video/mp4:
              schema:
                type: string
                format: binary
            video/webm:
              schema:
                type: string
                format: binary
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "502":
          description: Upstream IPFS gateway could not stream the content.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                error: Failed to stream from IPFS gateway
  /evidence/video:
    post:
      tags: [Evidence]
      summary: Upload video evidence to IPFS
      description: |-
        Accepts multipart form-data with a `file` field and a `tradeId` field.
        Supported MIME types: `video/mp4`, `video/webm`.
        Maximum file size: 50 MB.
        Supports optional `Idempotency-Key`.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                tradeId:
                  type: string
                  format: uuid
                file:
                  type: string
                  format: binary
              required: [tradeId, file]
      responses:
        "201":
          description: Evidence uploaded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceUploadResponse"
        "400":
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "413":
          description: Uploaded file exceeded the 50 MB limit.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                error: File exceeds the 50MB size limit
        "415":
          description: Unsupported media type.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                error: Unsupported media type — only MP4 and WebM are accepted
  /trades/{id}/history:
    get:
      tags: [Audit Trail]
      summary: Fetch a trade's audit history
      description: |-
        Default response is JSON. Use `format=csv` for a CSV export and `signed=true`
        to include a canonical payload and integrity signature metadata.
        Error code references: `AUTH_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
        - $ref: "#/components/parameters/HistoryFormatQuery"
        - $ref: "#/components/parameters/HistorySignedQuery"
      responses:
        "200":
          description: Audit history returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditHistoryResponse"
            text/csv:
              schema:
                type: string
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /trades/{id}/history/verify:
    get:
      tags: [Audit Trail]
      summary: Verify a signed audit history payload
      description: "Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `DOMAIN_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TradeIdPath"
        - $ref: "#/components/parameters/AuditSignatureQuery"
      responses:
        "200":
          description: Signature verification result returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditVerifyResponse"
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
              example:
                error: Missing signature query parameter
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/admin/auth/claims:
    get:
      tags: [Admin]
      summary: Inspect the caller's parsed admin JWT claims
      description: |-
        Diagnostic endpoint returning the sanitized JWT claims the backend
        parsed from the caller's bearer token (wallet address, token id,
        issuer/audience, issued/expiry timestamps), so an admin can verify
        exactly what the server sees without decoding the token by hand.
        Raw fields not meaningful to display (`sub`, `nbf`) are omitted.
        Error code references: `AUTH_ERROR`.
      security:
        - bearerAuth: []
        - adminAuth: []
      responses:
        "200":
          description: Sanitized admin JWT claims.
          content:
            application/json:
              schema:
                type: object
                properties:
                  walletAddress:
                    type: string
                  tokenId:
                    type: string
                  issuedAt:
                    type: string
                    nullable: true
                  expiresAt:
                    type: string
                    nullable: true
                  issuer:
                    type: string
                    nullable: true
                  audience:
                    type: string
                    nullable: true
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
  /api/admin/sessions/revoke:
    post:
      tags: [Admin]
      summary: Revoke an active admin session or token JTI
      description: |-
        Control plane endpoint to revoke active admin session JTIs.
      security:
        - bearerAuth: []
        - adminAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                jti:
                  type: string
                  description: Optional session JTI to invalidate (defaults to caller's JTI).
      responses:
        "200":
          description: Admin session revoked successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  jti:
                    type: string
                  revokedAt:
                    type: string
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
  /treasury/balance:
    get:
      tags: [Treasury]
      summary: Fetch the treasury balance
      description: |-
        Returns the current balance of the escrow contract treasury.
        Requires admin authorization via ADMIN_STELLAR_PUBKEYS env.
        Error code references: `AUTH_ERROR`, `INFRA_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Treasury balance retrieved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  balance:
                    type: string
                  asset:
                    type: string
                  contractId:
                    type: string
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
  /treasury/withdraw:
    post:
      tags: [Treasury]
      summary: Withdraw funds from the treasury
      description: |-
        Withdraws tokens from the escrow contract treasury.
        Only admin wallets can perform this operation.
        Error code references: `AUTH_ERROR`, `DOMAIN_ERROR`, `INFRA_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                destination:
                  type: string
                  description: Stellar public key to receive the funds.
                amount:
                  type: string
                  description: Amount to withdraw in USDC.
              required: [destination, amount]
      responses:
        "200":
          description: Withdrawal transaction built.
          content:
            application/json:
              schema:
                type: object
                properties:
                  unsignedXdr:
                    type: string
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/InternalError"
  /treasury/config:
    get:
      tags: [Treasury]
      summary: Fetch treasury configuration
      description: |-
        Returns the current treasury configuration including contract ID and network.
        Error code references: `AUTH_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Treasury configuration retrieved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  contractId:
                    type: string
                  network:
                    type: string
                  asset:
                    type: string
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
  /admin/contract/mediators:
    post:
      tags: [Admin]
      summary: Register a mediator on the escrow contract
      description: |-
        Builds an unsigned Soroban transaction that adds `mediatorAddress` to the
        on-chain mediator registry (`add_mediator`), previously only doable via the
        `soroban contract invoke` CLI. Admin-only; the caller's wallet is the
        transaction source/signer. As with other admin transaction endpoints, the
        backend never holds a signing key — the caller signs and submits the
        returned XDR themselves.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
        - adminAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                mediatorAddress:
                  type: string
                  description: Stellar public key to register as a mediator.
              required: [mediatorAddress]
      responses:
        "200":
          description: Add-mediator transaction built.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnsignedXdrResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/InternalError"
  /admin/contract/mediators/{address}:
    delete:
      tags: [Admin]
      summary: Revoke a mediator on the escrow contract
      description: |-
        Builds an unsigned Soroban transaction that removes `address` from the
        on-chain mediator registry (`remove_mediator`). Admin-only; the caller's
        wallet is the transaction source/signer.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
        - adminAuth: []
      parameters:
        - name: address
          in: path
          required: true
          description: Stellar public key to remove from the mediator registry.
          schema:
            type: string
      responses:
        "200":
          description: Remove-mediator transaction built.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnsignedXdrResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/InternalError"
  /admin/contract/fee:
    patch:
      tags: [Admin]
      summary: Update the escrow contract platform fee rate
      description: |-
        Builds an unsigned Soroban transaction that updates the platform fee
        (`update_fee_bps`). The contract itself enforces the fee stays within its
        configured [MIN_FEE_BPS, MAX_FEE_BPS] bounds; this endpoint additionally
        rejects out-of-range values before building a transaction. Admin-only;
        the caller's wallet is the transaction source/signer.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
        - adminAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                feeBps:
                  type: integer
                  minimum: 1
                  maximum: 500
                  description: New platform fee rate in basis points.
              required: [feeBps]
      responses:
        "200":
          description: Fee-update transaction built.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnsignedXdrResponse"
        "400":
          $ref: "#/components/responses/LegacyValidationError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/admin/streams/{id}/suspend:
    post:
      tags: [Admin]
      summary: Suspend a vesting stream
      description: |-
        Suspend a stream without immediate clawback. Gated by `ADMIN_ROUTES_ENABLED`.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
        - adminAuth: []
      parameters:
        - $ref: "#/components/parameters/StreamIdPath"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  maxLength: 500
      responses:
        "200":
          description: Stream suspended.
        "400":
          description: Invalid stream ID format or validation error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/admin/streams/{id}/resume:
    post:
      tags: [Admin]
      summary: Resume a suspended vesting stream
      description: |-
        Resume a previously suspended stream. Gated by `ADMIN_ROUTES_ENABLED`.
        Error code references: `AUTH_ERROR`, `VALIDATION_ERROR`, `INTERNAL_ERROR`.
      security:
        - bearerAuth: []
        - adminAuth: []
      parameters:
        - $ref: "#/components/parameters/StreamIdPath"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                note:
                  type: string
                  maxLength: 500
      responses:
        "200":
          description: Stream resumed.
        "400":
          description: Invalid stream ID format or validation error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /goals:
    get:
      tags: [Goals]
      summary: Fetch authenticated goal analytics
      description: "Error code references: `AUTH_ERROR`, `NOT_FOUND`, `INTERNAL_ERROR`."
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Goal analytics returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoalsAnalyticsResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/InternalError"
